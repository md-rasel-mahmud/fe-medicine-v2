import { Typography } from '@mui/material';
import { FC } from 'react';

const Home: FC = () => {
  return (
    <div>
      <Typography paragraph>Home Page</Typography>
    </div>
  );
};

export default Home;
